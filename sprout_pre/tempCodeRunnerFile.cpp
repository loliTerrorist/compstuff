clip = texts[i].substr(l,r-l);
            // cout<<"clip: "<< clip<<endl;
            // if(l==0) l=1;
            // // if(r==texts[i].size()-1) r = texts[i].size()-2;
            // cout<<"pre: "<<texts[i].substr(0,l-1)<<endl;
            // texts[i] = texts[i].substr(0,l-1)+texts[i].substr(r+1,texts[i].size()-r-1);
            // cout<<texts[i]<<endl;