#include <bits/stdc++.h>
#define int long long 
#define endl '\n'
using namespace std;

signed main(){
    int n; cin>>n;
    vector<int> ls(n);
    for(int i= 0; i<n  ; i++){
        cin>>ls[i];
    }
    vector<int> dpa(n);
    vector<int> dpb(n);
    

}